<table>
  <tbody>
  <tr>
    <th><%= t('label_tracker') %>:</th>
    <td>
      <%= select_tag 'settings[dine_tracker_id]', options_from_collection_for_select(Tracker.all, :id, :name, settings['dine_tracker_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.wating_status') %>:</th>
    <td>
      <%= select_tag 'settings[dine_wating_status_id]', options_from_collection_for_select(IssueStatus.all, :id, :name, settings['dine_wating_status_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.robot_status') %>:</th>
    <td>
      <%= select_tag 'settings[dine_robot_status_id]', options_from_collection_for_select(IssueStatus.all, :id, :name, settings['dine_robot_status_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.success_robot_status') %>:</th>
    <td>
      <%= select_tag 'settings[dine_success_robot_status_id]', options_from_collection_for_select(IssueStatus.all, :id, :name, settings['dine_success_robot_status_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.error_robot_status') %>:</th>
    <td>
      <%= select_tag 'settings[dine_error_robot_status_id]', options_from_collection_for_select(IssueStatus.all, :id, :name, settings['dine_error_robot_status_id']), include_blank: true %>
    </td>
  </tr>
  </tbody>
</table>

<hr />

<% @issue_custom_fields = IssueCustomField.all %>
<table>
  <tbody>
  <tr>
    <th><%= t('export_to_vale_sap_robot.referencia') %>:</th>
    <td>
      <%= select_tag 'settings[dine_referencia_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_referencia_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.valor_total_nf') %>:</th>
    <td>
      <%= select_tag 'settings[dine_valor_total_nf_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_valor_total_nf_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.cnpj_fornecedor') %>:</th>
    <td>
      <%= select_tag 'settings[dine_cnpj_fornecedor_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_cnpj_fornecedor_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.pedido') %>:</th>
    <td>
      <%= select_tag 'settings[dine_pedido_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_pedido_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.modo_pagamento') %>:</th>
    <td>
      <%= select_tag 'settings[dine_metodo_pagamento_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_metodo_pagamento_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.emissao') %>:</th>
    <td>
      <%= select_tag 'settings[dine_emissao_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_emissao_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.vencimento') %>:</th>
    <td>
      <%= select_tag 'settings[dine_vencimento_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_vencimento_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.empresa') %>:</th>
    <td>
      <%= select_tag 'settings[dine_empresa_id]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_empresa_id']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.codigo_barras') %>:</th>
    <td>
      <%= select_tag 'settings[dine_codigo_barras]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_codigo_barras']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.postagem') %>:</th>
    <td>
      <%= select_tag 'settings[dine_postagem]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_postagem']), include_blank: true %>
    </td>
  </tr>
  <tr>
    <th><%= t('export_to_vale_sap_robot.tipo_documento') %>:</th>
    <td>
      <%= select_tag 'settings[dine_tipo_documento]', options_from_collection_for_select(@issue_custom_fields, :id, :name, settings['dine_tipo_documento']), include_blank: true %>
    </td>
  </tr>
  </tbody>
</table>
